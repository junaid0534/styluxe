name: styluxe
description: "A new Flutter project."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.0.0
  image_picker: ^1.1.2
  uuid: ^4.4.0
  flutter_animate: ^4.5.0
  google_maps_flutter: ^2.9.0
  geolocator: ^12.0.0
  cupertino_icons: ^1.0.8
  font_awesome_flutter: ^11.0.0
  fl_chart: ^1.2.0
  google_fonts: ^6.2.1     # ← Added for Poppins

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true